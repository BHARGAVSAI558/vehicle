services:
  - type: web
    name: vehicle-frontend
    env: node
    buildCommand: cd vehicle && npm install && npm run build
    staticPublishPath: vehicle/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: 18 